spring:
  datasource:
    url: jdbc:postgresql://localhost:5332/takenote
    username: goodcode
    password: password
  jpa:
    hibernate:
      ddl-auto: update
#      ddl-auto: create-drop
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true
    show-sql: true